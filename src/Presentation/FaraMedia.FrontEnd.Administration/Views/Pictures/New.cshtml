@using System.Web.Mvc.Html
@using FaraMedia.Data.Schemas.Layout.Sections
@using FaraMedia.Web.Framework.Mvc.Extensions
@using FaraMedia.Web.Framework.Mvc.UI
@using FaraMedia.Web.Framework.Routing
@using FaraMedia.Web.Framework.UI
@model FaraMedia.FrontEnd.Administration.Models.FileManagement.Editable.EditablePictureModel
@{
    Layout = "_PicturesLayout.cshtml";
    Html.AddLocalizedTitleParts(MediaSectionConstants.PicturesController.New.Metadata.Title);
    Html.AddBreadcrumbParts(Html.LocalizedRouteLink(MediaSectionConstants.PicturesController.New.Metadata.Title, MediaSectionConstants.PicturesController.New.RouteName));
}
<section>
    <div class="page-header">
        <h1>
            @Html.T(MediaSectionConstants.PicturesController.New.Metadata.Title) 
            <small>(@Html.LocalizedBackToLink(MediaSectionConstants.PicturesController.List.Metadata.Title, MediaSectionConstants.PicturesController.List.RouteName, MediaSectionConstants.PicturesController.List.Defaults))</small>
        </h1>
    </div>
    @using (Html.Custom().BeginRouteForm(MediaSectionConstants.PicturesController.New.RouteName)) {
        @Html.Custom().ValidationSummary(true)
        <div class="row">
            <div class="span4">
                <h2>@Html.T(MediaSectionConstants.PicturesController.New.Metadata.Title)</h2>
                <p>@Html.T(MediaSectionConstants.PicturesController.New.Metadata.Description)</p>
            </div>
            <div class="span12">
                @Html.Partial("_NewOrEdit", Model)
            </div>
        </div>
        @Html.Partial("_NewActions")
    }
</section>